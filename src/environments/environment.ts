/**
 * Production settings. Update `apiBaseUrl` to match your deployed API origin.
 */
export const environment = {
  production: true,
  apiBaseUrl: 'https://azureinsightsmonitoringapi.azurewebsites.net',
};
